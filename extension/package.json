{
  "name": "typesmart-extension",
  "version": "1.0.0",
  "description": "TypeSmart Browser Extension",
  "scripts": {
    "build": "node build.js",
    "build:chrome": "node build.js --target=chrome",
    "build:firefox": "node build.js --target=firefox",
    "dev": "node build.js --watch"
  },
  "devDependencies": {
    "fs-extra": "^11.1.1"
  }
}
